
<h2>All fixtures in DB:</h2>

<% @fixtures.each do |fixture| %>
    • <b> <%= fixture.competseason.competition.name %> Round <%= fixture.round %> </b> |
    <%= link_to fixture_path(fixture.id) do %>
        <%= fixture.hometeam.name %> vs. <%= fixture.awayteam.name %>
    <%end%> <br>
<%end%>
